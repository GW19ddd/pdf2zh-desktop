# Zotero 联动功能 — Windows 手工测试指引

> 适用版本：pdf2zh-desktop Windows 版（v2.3.x）
> 测试依据：`docs/zotero-integration-guide-for-win.md` 第八节「测试验证清单」
> 使用方法：按序执行下列测试项，每项填写结果与备注，全部完成即验收。

---

## 〇、测试环境准备

### 1. 准备测试文献
- 启动 Zotero（建议用独立测试账号/空库，避免污染正式库）
- 新建测试条目，附加 1 篇 PDF（任意论文 PDF 即可）
- 确认 PDF 被存进 Zotero 数据目录：`<数据目录>\storage\<8位key>\xxx.pdf`
- 记下该 `<8位key>`（如 `KSII2GGN`），后续测试会用到

### 2. 确认 pdf2zh 版本
- 启动 pdf2zh，底部「关于」页确认版本号 ≥ v2.3.7

### 3. 准备翻译服务
- 设置页配置可用的翻译服务（Google / DeepL / 其他），确保能正常翻译

### 4. 可选：安装 Zotero 插件（测试第 8 项需要）
- 方式 A：pdf2zh 设置页 → Zotero 联动 →「一键安装 Zotero 插件」
- 方式 B：Zotero → 工具 → 附加组件 → 齿轮 → 从文件安装插件 → 选择 `assets/pdf2zh-connector.xpi`，重启 Zotero

> 记录：Zotero 数据目录位置：________；测试 PDF 的 8 位 key：________

---

## 一、核心回写测试（前 7 项）

### 测试 1：拖拽识别 + 提示条
**前置：** 在 Zotero 中右键 PDF 附件 →「在文件管理器中显示」，得到 Zotero storage 目录下的真实文件路径

| 步骤 | 操作 |
|---|---|
| 1 | 从资源管理器（Explorer）把该 PDF **直接拖入** pdf2zh 翻译页 |
| 2 | 观察文件列表上方是否出现蓝色提示条「📚 Zotero 文献 · 译文自动保存回原位」 |

**预期：** 出现提示条 ✔ / 未出现 ✘
**备注：** ________________________________

### 测试 2：翻译完成 → 译文回写原目录
**前置：** 测试 1 的文件已加入列表，默认设置（输出格式 side_by_side，保留副本开）

| 步骤 | 操作 |
|---|---|
| 1 | 点击「开始翻译」，等待完成 |
| 2 | 打开 Zotero storage 中该 PDF 所在目录（`storage\<8位key>\`） |

**预期：** 原目录出现译文文件（`xxx-side_by_side.pdf`）✔ / 未出现 ✘
**备注：** 文件名为：____________

### 测试 3：Zotero 中刷新可见附件
**前置：** 完成测试 2

| 步骤 | 操作 |
|---|---|
| 1 | 在 Zotero 中选中该条目，按 F5 或重启 Zotero 刷新 |

**预期：** 条目下能看到新的 PDF 附件（译文）✔ / 看不到 ✘
**备注：** ________________________________

### 测试 4：设置页勾选 Dual + Mono → 回写多个译文
| 步骤 | 操作 |
|---|---|
| 1 | 设置页 → Zotero 联动：勾选「双语对照 Dual」+「仅翻译 Mono」（保留 side_by_side） |
| 2 | 重新拖入另一个 Zotero PDF，翻译 |
| 3 | 检查该 PDF 的 Zotero storage 目录 |

**预期：** 原目录出现 `-dual` 与 `-mono` 两个译文文件（共 3 个）✔ / 数量不对 ✘
**备注：** 实际出现：____________

### 测试 5：取消「保留副本」→ 输出目录无副本
| 步骤 | 操作 |
|---|---|
| 1 | 设置页 → Zotero 联动：取消勾选「保留本地副本」 |
| 2 | 再拖入一个 Zotero PDF 翻译 |
| 3 | 检查默认输出目录（`~/Documents/pdf2zh_files`）与 Zotero 原目录 |

**预期：** Zotero 原目录有译文、输出目录无该译文副本 ✔ / 不符 ✘
**备注：** ________________________________

### 测试 6：拖非 Zotero 的 PDF → 走正常流程
| 步骤 | 操作 |
|---|---|
| 1 | 从 Explorer 拖入任意**非 Zotero** 目录的 PDF（如下载目录） |

**预期：** 不显示 Zotero 提示条，译文只输出到默认目录，不产生回写 ✔ / 不符 ✘
**备注：** ________________________________

### 测试 7：Zotero 未运行时翻译 → 回写不依赖插件
**前置：** 完全退出 Zotero（确认托盘无 Zotero 图标）

| 步骤 | 操作 |
|---|---|
| 1 | 从 Explorer 拖入一个 Zotero storage 里的 PDF（路径含 `storage\8位key\` 即可） |
| 2 | 翻译 |
| 3 | 检查原 storage 目录 |

**预期：** 无插件也能完成：译文复制回原目录（兜底复制），不报错、不闪退 ✔ / 不符 ✘
**备注：** 界面提示信息为：____________

---

## 二、插件联动测试（需插件）

### 测试 8：安装插件 → 译文自动关联为 Zotero 附件
**前置：** 已按准备步骤 4 安装 `pdf2zh Connector` 插件，Zotero 正在运行

| 步骤 | 操作 |
|---|---|
| 1 | 拖入 Zotero storage 里的 PDF 并翻译 |
| 2 | 翻译完成后查看 Zotero 中该条目 |

**预期：** 译文自动成为该条目的子附件（附件名含「译文」），无需手动导入 ✔ / 未关联 ✘
**备注：** 界面提示：____________；Zotero 附件名：____________

### 测试 8b：拖拽条目（MIME 解析，增强功能）
| 步骤 | 操作 |
|---|---|
| 1 | 直接从 Zotero 窗口**拖条目**（不是拖文件）到 pdf2zh 翻译页 |
| 2 | 观察是否自动解析出 PDF |

**预期：** 自动识别条目下的 PDF 加入翻译列表（zotero/item MIME 生效）；若失败，回退为拖文件方式即可 ✔ / 不符 ✘
**备注：** ________________________________

---

## 三、稳定性测试

### 测试 9：快速连续拖入多个文件 → 不闪退，批量回写
| 步骤 | 操作 |
|---|---|
| 1 | 准备 3~5 个 Zotero PDF（不同条目） |
| 2 | **快速连续**拖入 pdf2zh（尽量在 1 秒内完成） |
| 3 | 点击翻译，等待全部完成 |

**预期：** 不闪退、不崩溃；全部翻译成功且译文都回写/关联到各自原条目 ✔ / 异常 ✘
**备注：** 拖入几个：____；成功几个：____

---

## 四、附加检查（Win 特有，建议测试）

### 测试 10：链接附件（zotmoov 等移出 storage 的 PDF）
> 仅当使用 zotmoov / 手动移动过附件时有意义，可跳过
| 步骤 | 操作 |
|---|---|
| 1 | 若 Zotero 中某 PDF 不在 storage 内（自定义目录），拖入该 PDF 翻译 |

**预期：** 译文仍能关联/回写（通过 SQLite 反查 key），界面显示成功提示 ✔ / 不符 ✘
**备注：** ________________________________

### 测试 11：自定义数据目录
> 仅当用户在 Zotero 偏好改过「数据存储位置」时有意义，可跳过
| 步骤 | 操作 |
|---|---|
| 1 | 若 Zotero 数据目录非默认 `~/Zotero`，重复测试 1~3 |

**预期：** 功能全部正常（`_find_zotero_data_dir` 读取 prefs.js 定位）✔ / 不符 ✘
**备注：** ________________________________

---

## 五、结果汇总表

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 拖拽识别 + 提示条 | ☐ 通过 ☐ 失败 | |
| 2 | 译文回写原目录 | ☐ 通过 ☐ 失败 | |
| 3 | Zotero 刷新可见 | ☐ 通过 ☐ 失败 | |
| 4 | Dual + Mono 多格式 | ☐ 通过 ☐ 失败 | |
| 5 | 取消保留副本 | ☐ 通过 ☐ 失败 | |
| 6 | 非 Zotero 走正常流程 | ☐ 通过 ☐ 失败 | |
| 7 | Zotero 未运行回写 | ☐ 通过 ☐ 失败 | |
| 8 | 插件自动关联附件 | ☐ 通过 ☐ 失败 | |
| 8b | 拖拽条目 MIME | ☐ 通过 ☐ 失败 | |
| 9 | 批量快速拖入不闪退 | ☐ 通过 ☐ 失败 | |
| 10 | 链接附件（可选） | ☐ 通过 ☐ 失败 ☐ 跳过 | |
| 11 | 自定义数据目录（可选） | ☐ 通过 ☐ 失败 ☐ 跳过 | |

**总评：** 通过 ____ 项 / 失败 ____ 项
**测试人：** ________　**日期：** ________

> 若某项失败，请附：界面截图、`logs/` 下日志、Zotero 数据目录结构，反馈到 issue 或提交给开发。
